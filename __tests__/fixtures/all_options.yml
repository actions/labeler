label1:
  - any:
      - changed-files:
          - AnyGlobToAnyFile: ['glob']
      - head-branch: ['regexp']
      - base-branch: ['regexp']
  - all:
      - changed-files:
          - AllGlobsToAllFiles: ['glob']
      - head-branch: ['regexp']
      - base-branch: ['regexp']

label2:
  - changed-files:
      - AnyGlobToAnyFile: ['glob']
  - head-branch: ['regexp']
  - base-branch: ['regexp']
